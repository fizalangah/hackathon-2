# Contracts

This is a CLI application that operates entirely in memory. As such, there are no external API contracts (e.g., OpenAPI, GraphQL schemas) required for this feature. All interactions are handled directly through the command-line interface.